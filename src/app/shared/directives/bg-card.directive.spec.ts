import { BgCardDirective } from './bg-card.directive';

describe('BgCardDirective', () => {
  it('should create an instance', () => {
    const directive = new BgCardDirective();
    expect(directive).toBeTruthy();
  });
});
